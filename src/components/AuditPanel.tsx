import type { AuditResult } from "../lib/audit";
export default function AuditPanel({audit}:{audit:AuditResult}){return <section className="rounded-xl border border-zinc-800 bg-zinc-900/60 p-4"><div className="mb-3 flex items-center justify-between"><h3 className="text-xs font-semibold uppercase tracking-wider text-zinc-400">Provenance audit</h3><span className="text-[10px] text-zinc-500">{audit.fileType} · {(audit.fileSize/1024).toFixed(1)} KB</span></div><div className="space-y-2">{audit.findings.map(f=><div key={f.id} className="rounded-lg border border-zinc-800 p-3"><div className="flex items-center gap-2 text-sm"><span className={f.status==="found"?"text-red-400":f.status==="warn"?"text-amber-400":"text-emerald-400"}>{f.status==="found"?"●":f.status==="warn"?"◆":"✓"}</span><span className="font-medium text-zinc-200">{f.label}</span></div><p className="mt-1 text-xs leading-relaxed text-zinc-500">{f.detail}</p></div>)}</div></section>}
