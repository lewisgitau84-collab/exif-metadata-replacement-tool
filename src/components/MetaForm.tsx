import { useMemo } from "react";
import { CAMERA_PRESETS, ISO_STEPS, exposureProgramNames, lightSourceNames, meteringModeNames, type MetaFields } from "../types";
export default function MetaForm({fields,onChange,imgWidth,imgHeight}:{fields:MetaFields;onChange:(v:MetaFields)=>void;imgWidth:number;imgHeight:number}){const set=(k:keyof MetaFields,v:any)=>onChange({...fields,[k]:v});const iso=useMemo(()=>ISO_STEPS.reduce((a,b)=>Math.abs(b-fields.iso)<Math.abs(a-fields.iso)?b:a),[fields.iso]);return <div className="space-y-4"><section className="rounded-xl border border-zinc-800 bg-zinc-900/60 p-4"><h2 className="mb-3 text-sm font-semibold">Camera profile</h2><select className="w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm" defaultValue="" onChange={e=>{const p=CAMERA_PRESETS.find(x=>x.id===e.target.value);if(p)onChange({...fields,make:p.make,model:p.model,lens:p.lens,software:p.software,iso:p.iso,shutter:p.shutter,aperture:p.aperture,focalLength:p.focal,focal35mm:p.focal35mm})}}><option value="">Choose a preset…</option>{CAMERA_PRESETS.map(p=><option key={p.id} value={p.id}>{p.label}</option>)}</select></section><section className="grid grid-cols-2 gap-3 rounded-xl border border-zinc-800 bg-zinc-900/60 p-4"><h2 className="col-span-2 text-sm font-semibold">Camera & capture</h2>{([['make','Make'],['model','Model'],['lens','Lens'],['software','Software / firmware'],['shutter','Shutter (seconds)'],['aperture','Aperture (f)'],['focalLength','Focal length (mm)'],['artist','Artist / author'],['copyright','Copyright']] as const).map(([k,l])=><label key={k} className="text-xs text-zinc-400">{l}<input className="mt-1 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm text-zinc-200" value={String(fields[k])} onChange={e=>set(k,e.target.value)} /></label>)}<label className="text-xs text-zinc-400">ISO<select className="mt-1 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm" value={iso} onChange={e=>set('iso',Number(e.target.value))}>{ISO_STEPS.map(x=><option key={x}>{x}</option>)}</select></label><label className="text-xs text-zinc-400">Exposure program<select className="mt-1 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm" value={fields.exposureProgram} onChange={e=>set('exposureProgram',Number(e.target.value))}>{exposureProgramNames.map((x,i)=><option key={x} value={i}>{x}</option>)}</select></label><label className="text-xs text-zinc-400">Metering<select className="mt-1 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm" value={fields.meteringMode} onChange={e=>set('meteringMode',Number(e.target.value))}>{meteringModeNames.map((x,i)=><option key={x} value={i}>{x}</option>)}</select></label><label className="text-xs text-zinc-400">Light source<select className="mt-1 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2 text-sm" value={fields.lightSource} onChange={e=>set('lightSource',Number(e.target.value))}>{lightSourceNames.map((x,i)=><option key={x} value={i}>{x}</option>)}</select></label><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={fields.flash} onChange={e=>set('flash',e.target.checked)}/>Flash fired</label><label className="flex items-center gap-2 text-xs"><input type="checkbox" checked={fields.gpsInclude} onChange={e=>set('gpsInclude',e.target.checked)}/>Embed GPS</label>{fields.gpsInclude&&<><label className="text-xs text-zinc-400">Latitude<input className="mt-1 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2" value={fields.gpsLat} onChange={e=>set('gpsLat',e.target.value)}/></label><label className="text-xs text-zinc-400">Longitude<input className="mt-1 w-full rounded-lg border border-zinc-700 bg-zinc-950 px-3 py-2" value={fields.gpsLon} onChange={e=>set('gpsLon',e.target.value)}/></label></>}<p className="col-span-2 text-[11px] text-zinc-500">Output: JPEG {imgWidth}×{imgHeight}. Re-encoding may slightly change file size.</p></section></div>}
